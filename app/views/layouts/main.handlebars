<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>{{{title}}}</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <link rel="stylesheet" type="text/css" href="css/master.css">
  </head>
  <body>
    <nav class="navbar navbar-expand-sm navbar-light bg-light">
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav">
          <li class="nav-item active">
            <a class="nav-link" href="/">Home </a>
          </li>
        </ul>
        <form class="form-inline my-2 my-lg-0">
          <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search" name="query">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
        </form>
        <ul class="navbar-nav ml-auto">
          <li class="nav-item" id="reglog">
            <a class="nav-link cust-dropdown" href="javascript:void(0)">{{#if always.username}}{{always.username}}{{else}}Not logged in{{/if}}</a>
            {{#unless always.username}}
              {{>reglog_menu}}
            {{/unless}}
          </li>
          <li class="nav-item">
            {{#if always.username}}
              <a class="nav-link" href="/api/users/logout">Logout</a>
            {{/if}}
          </li>
        </ul>
      </div>
    </nav>
    {{{body}}}

    <script src="https://code.jquery.com/jquery-3.4.0.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
    {{#unless always.username}}
      <script src="js/user.js"></script>
    {{/unless}}
    <script src="js/main.js"></script>
  </body>
</html>
