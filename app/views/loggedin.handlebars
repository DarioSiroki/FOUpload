<div class="row maxheight">
    <div class="col-8 offset-2 maxheight">
        <h1 class="text-center">My files</h1>
        <form id="uploadform" class="maxheight">
            {{#if filetable}}
            <table class="table">
                <thead>
                    <tr>
                    <th scope="col">#</th>
                    <th scope="col">Filename</th>
                    <th scope="col">Uploaded on</th>
                    <th scope="col" class="text-center">Options</th>
                    </tr>
                </thead>
                <tbody>
                    {{{filetable}}}
                </tbody>
            </table>
            {{else}}
                <p class="text-center">You have no files. Drag or click anywhere to upload. </p>
            {{/if}}
            <input type="hidden" accept="image/*">

            
        </form>

    </div>
</div>
